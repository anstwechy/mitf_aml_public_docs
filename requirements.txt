# Pinned for reproducible doc builds; bump intentionally when upgrading the theme.
mkdocs-material==9.5.49
mkdocs==1.6.1
# Injects Mermaid.js + init (Material community theme does not ship the Mermaid runtime).
mkdocs-mermaid2-plugin==1.2.3
